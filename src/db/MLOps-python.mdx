---
title: "What is MLOps?"
description: "A step-by-step guide for setting up an MLOps pipeline end-to-end using Python."
date: "2026-02-01"
author: "Maximilian K. Posnik"
keywords: "Python, MLOps, scripting, productivity"
collection: "AI/ML"
---

import { extract, transform, load } from '@/lib/code-examples';

# What is MLOps?

## A step-by-step guide for setting up an MLOps pipeline end-to-end using Python.

The field of Machine Learning has many domains for a budding Machine Learning Engineer to dive into. You can program machine learning libraries from scratch, or you can deploy, monitor, and maintain ML models at scale. The latter is frequently called MLOps in job descriptions in 2026. The following guide demonstrates the role of a Machine Learning Engineer specializing in MLOps in a real-world scenario.

## Defining the Problem

An emergency room is located in a linguistically diverse area. Hospital staff are having difficulty triaging patients because they are unable to properly identify the needs of the patient. The ML team has developed a new model: a Multinomial Naive Bayes classifier, to read input from patients in their native language and output the correct specialty (Cardiology, Neurology, etc.) that the patient needs to see. The ML team is asking us to handle the end-to-end MLOps pipeline for the triage system.

## 1 - Project Initialization & Data Preprocessing

The first step is formally called data preprocessing, which is when we 'wrangle & clean' the data, or to put it simply: prepare the data for the ML model. This step might sound less meaningful than it really is, when in reality, the result heavily depends on what is done at this step. The ML model we are using (Multinomial Naive Bayes), like any ML model, is just a mathematical algorithm, a way to process the data computationally. The model we are using is calculating probabilities based on word frequencies. Without data, the model cannot produce any result. The machine learning model will choose its response based on the data that it was trained on. For this guide, the model will be trained on the `Symptom2Disease` dataset. This dataset contains 24 unique disease labels, each paired with a text description. We will use this data to train the model to predict disease labels for symptoms recorded from emergency room patients. 
We also need to add two utilities: 
* A way to take the disease label and assign it to the correct specialist (hypertension -> cardiologist)
* A translator to change all patient input to English to align with the dataset

<CloudinaryImage 
  src="https://res.cloudinary.com/dl6exbzi2/image/upload/v1769989125/MLOps-python-02_enh9rd.png" 
  alt="A screenshot of a section of the Symptom2Disease dataset."
  width={800}
  height={500}
/>

## 1.1 - Project Setup

<CloudinaryImage 
  src="https://res.cloudinary.com/dl6exbzi2/image/upload/v1769989112/MLOps_in_python_01.png" 
  alt="A screenshot of what the project folder structure looks like at initiliazation."
  width={500}
  height={500}
/>

Before we can run any scripts the project must be properly intialized. Create a new python project and `.venv` in your IDE of choice.
1. In the project root create the following directories: `artifacts`, `data`, and `ETL`.
2. Within the `data` directory, create 2 more folders: `processed` & `raw`.
3. Place `Symptom2Disease.csv` (link found below) into the `raw` folder.
4. In the root of the project create a new file called `requirements.txt` and place the following Dependencies inside:
```
# Core ETL Pipeline Dependencies 
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# Translation & NLP
deep-translator>=1.11.4
langdetect>=1.0.9

# Visualization 
matplotlib>=3.7.0
seaborn>=0.12.0

# Web Dashboard 
streamlit>=1.28.0

# Development & Testing
pytest>=7.4.0
```
5. Activate your environment (if you haven't already):
- Mac/Linux: `source .venv/bin/activate`
- Windows: `.venv\Scripts\activate`
6. In the terminal run: `pip install -r requirements.txt`


## 1.2 - Extract

**What are we doing?**

We are extracting the data from the CSV file and returning it as a Pandas DataFrame. In addition, we are also performing integrity checks by verifying the file exists and there are no missing columns. Then we are using built-in Pandas methods to log data metrics. 

**Why are we doing this?**

Pandas has many built-in tools and features to make data preprocessing much easier. By storing the data in a DataFrame we can wrangle the data quick and easy, without having to write too much code. We can verify that our data structure is valid by checking for the `Symptom2Disease` 'label' and 'text' columns. We are doing this during the first step of our ETL pipeline, because we need to ensure the data is valid before we transform it. Detailed logging ensures we can see exactly what data is being entered into the pipeline.

The following python script `./ETL/extract.py` is used to extract data from the `Symptom2Disease.csv` CSV file:

(Click on the highlighted line numbers to view annotations)
<AnnotatedCode
  code={extract.code}
  language="python"
  startLine={1}
>
  <Annotation id="imports" lines="1-3" title="Importing the Dependencies">
    Importing the required dependencies:
    - **pandas** is being used to read from the CSV and to provide a DataFrame (`df`) structure and utilities. 
    - **os** is being used to verify the filepath storing `Symptom2Disease.csv` exists.
    - **tuple** is being used as a static type hint (not enforcer); helps catch coding/typing mistakes before you run the program, but it cannot detect problems that depend on the actual CSV file contents or runtime I/O.
    
  </Annotation>

  <Annotation id="extract-symtom-data" lines="5" title="Extract Symptom Data Function">
    This function returns the data in the CSV as a Pandas dataframe as well as a summary of the data.
  </Annotation>

  <Annotation id="file-not-found-error" lines="7-8" title="FileNotFoundError">
    Checking if the filepath exists.
  </Annotation>

  <Annotation id="pd-read-csv" lines="10" title="Pandas read_csv method">
    This is calling pandas `read_csv` function on the file passed to `filepath`.

    `index_col=0` tells pandas to use the first column of `Symptom2Disease.csv` as the dataframe's index (the 'label' column). By default pandas uses a numeric index (0, 1, 2, ...).
  </Annotation>

  <Annotation id="list-comprehension" lines="13-16" title="List Comprehension">
    This code looks more complex than it is because it contains a **List Comprehension**. 
    
    Put simply: take the predefined `required_columns` list and loop through it (`for col in required_columns`). Compare the required columns to the columns in the dataframe `df`. If one or both of the required columns is not in the dataframe `df`, then store the missing column(s) in `missing_columns`. If `missing_columns` exist then raise an error.
  </Annotation>

  <Annotation id="total-rows" lines="22" title="Total Rows in the DataFrame">
    The total number of rows in the DataFrame is equivalent to the length of the DataFrame `len(df)`
  </Annotation>

  <Annotation id="total-columns" lines="23" title="Total Columns in the DataFrame">
    The total number of columns in the DataFrame.
  </Annotation>

  <Annotation id="column-names" lines="24" title="List of Column Names">
    The names of the columns in the pandas DataFrame.
  </Annotation>

  <Annotation id="null-counts" lines="25" title="Total Number of Null Values">
    The number of null values in each column. `to_dict()` converts the result to this format: `{'label': 0, 'text': 5}`.
  </Annotation>

  <Annotation id="unique-labels" lines="26" title="Total Unique Disease Labels">
    `.nunique()` counts the number of unique 'label' values (different disease names in the dataset). The conditional if statement is a safety check to verify the 'label' column exists.
  </Annotation>

  <Annotation id="dtype" lines="27" title="Data Type of Each Column">
    Stores the data type of each column as a dict like: `{'label': 'object', 'text': 'object'}`
  </Annotation>

  <Annotation id="return-dfsummary" lines="36" title="Return DataFrame and Summary">
    The function returns the CSV data converted to a DataFrame and a summary of the data.
  </Annotation>
</AnnotatedCode>

## 1.3 - Transform

**What are we doing?**

In the extract phase we logged how many null values were in our extacted DataFrame, but we did not do anything to clean the data. In the transform phase we are removing null values and duplicate values. Then we are fitting the cleaned data to our TF-IDF Vectorizer. 

**Why are we doing this?**

We need to prepare the raw symptom text data into a format that machine learning models can actually use. ML models can't work with text directly - they need numbers. Additionally, we need to ensure data quality before training begins.

<AnnotatedCode
  code={transform.code}
  language="python"
  startLine={1}
>

  <Annotation id="python-regex" lines="2" title="Python's Regular Expression Module">
    Used for pattern matching.
  </Annotation>

  <Annotation id="tfidf-import-statement" lines="4" title="Term Frequency Inverse Document Frequency Vectorizer">
    Importing the term frequency inverse document frequency vectorizer from the scikit-learn library.
  </Annotation>

  <Annotation id="uncleaned-df-len" lines="9" title="Initial Rows">
    The number of rows in the DataFrame before it is cleaned. The `len()` of a DataFrame is equal to the number of rows it has.
  </Annotation>

  <Annotation id="initializing-log-values" lines="10-15" title="Initializing Log">
    These are the values we are going to track and log. 
  </Annotation>

  <Annotation id="null-count-before" lines="18" title="Null Count Before Cleaning">
    In the extract phase we did something similar only we checked for null counts in an entire DataFrame and returned a comprehensive dict structure individually listing the null counts of each column. Here, we are only checking for null counts specifically in the columns 'text' and 'label'. Then we are returning a sum of the total amount of null values in these combined columns. This is to show how many total null values we are removing.
  </Annotation>

  <Annotation id="drop-null-values" lines="22" title="Deleting Rows With Missing Values">
    If a row has a missing value in either the 'text' column, 'label' column, or both then the entire row is deleted. The original `df` extracted from the CSV stays the same. `df_cleaned` is a brand new DataFrame (Pandas allocates a new block of memory) with the null values removed by using the `.dropna()` method.
  </Annotation>

  <Annotation id="logging-number-nulls-removed" lines="24" title="Logging Rows Removed">
    To log the number of rows that were removed, we substract the `len()` of the the new DataFrame (`df_cleaned`) from the original DataFrame (`df`).  
  </Annotation>

  <Annotation id="conditional-null-row-removed-check" lines="26-29" title="Checking if Rows Were Removed">
    Simple conditional statement to check if any null values were removed. If there were no null values removed we should print "No null values found."
  </Annotation>

  <Annotation id="duplicate-values-check" lines="32" title="Checking for Duplicate Symptom Entries">
    We know that there are 24 unique 'label' values representing the diseases in our dataset, therefore we only need to check the 'text' column for duplicates. We use Pandas' `.duplicated` method to check the 'text' column and print the sum of total duplicate values.
  </Annotation>

  <Annotation id="dropping-text-duplicates" lines="35-37" title="Dropping Duplicates and Logging">
    We use `.drop_duplicates()` on the 'text' column keeping only the first duplicate value. `df_cleaned` now has both null values and duplicate values removed. The reason we created `duplicates_before` was to log how many duplicate values were in our DataFrame. Then we use the same process as the null values, simply subtracting the new length of the DataFrame from the previous. 
  </Annotation>

  <Annotation id="conditional-check-if-duplicates-removed" lines="39-42" title="Conditional Duplicates Removed Check">
    This is the same check that we did for the null values. We are checking if any duplicate values were removed and printing the number that were removed. If no duplicates were found then we will print "no duplicates found."
  </Annotation>

  <Annotation id="equal-to-df_cleaned" lines="44" title="Logging Number of Rows After Cleaning">
    This value is equal to `len(df_cleaned)` after removing the null and duplicate values. 
  </Annotation>

  <Annotation id="df_cleaned-reset-index" lines="47" title="Resetting the Index">
    When we drop rows from the original DataFrame we extracted from the CSV, the original index remains the same. This creates gaps in our DataFrame, so we cannot forget to reset the index. `reset_index(drop=True)` creates a new ordered index and simultaneously removes the old index. 
  </Annotation>

  <Annotation id="printing-log-and-returning-cleaned-df" lines="49-56" title="Printing Our Log and Returning the Cleaned DataFrame">
    The retention rate is equal to the final number of rows divided by the number of rows we started with. We print the cleaning summary statistics and return the cleaned DataFrame.
  </Annotation>

  <Annotation id="cleaing-symptom-text" lines="59" title="Cleaning Up Text in Symptom Data">
    This function is used to clean up the actual text in the dataset by removing any unwanted characters or extra whitespace. 
  </Annotation>

  <Annotation id="removing-unwanted-characters-using-regex" lines="67" title="Removing Unwanted Characters Using Regex Patterns">
    Remove anything that's NOT in the allowed list. 
    - [^...] = "NOT these characters" (the ^ means "not")
    - a-z means all lowercase letters a-z are allowed.
    - All digits 0-9 are allowed.
    - ,!?' punctuation is allowed.
    - Anything that is not these character is replaced by `' '` meaning it is removed.
  </Annotation>

  <Annotation id="calling-clean-data" lines="78" title="Calling clean_data()">
    Now we are calling the actual function we created on line 7 on the extracted `df`. 
  </Annotation>

  <Annotation id="calling-preprocess-text" lines="81" title="Calling Preprocess Text">
    Calling the preprocess_text function on the 'text' column of our DataFrame and saving the results. 
  </Annotation>

  <Annotation id="creating-vectorizer-class" lines="86-92" title="Creating Our Custom Vectorizer">
    Creating an instance of the TfidfVectorizer class with custom configuration:
    - `max_features=5000`: We only keep the top 5000 most frequent/important unique words in the text.
    - `stop_words='english'`: Automatically filters out common English words like: "the", "a", "is", "and", "of", "to" because they don't add any meaning to the data.
    - `ngram_range=(1, 2)`: Extracts both single words and two-word phrases such as "red rash", "itchy skin", and "sore throat." Two-word combinations often carry more meaning than single words.
    - `min_df=2`: CAUTION, up until now 'df' has meant DataFrame but this parameter refers to 'Document Frequency.' This means a word must appear in at least 2 documents (symptoms). This helps to filter out outliers.
    - `max_df=0.95`: If a word appears in 95%+ of all symptom texts, it's ignored because words that appear everywhere don't help distinguish between symptoms.

  </Annotation>

  <Annotation id="fitting-tf-idf" lines="96" title="Fitting the Vectorizer to the Data">
    This is how the Vectorizer reads the cleaned symptom text data:
    - It reads through every symptom description.
    - Builds a dictionary of all unique words/phrases.
    - Calculates statistical weights for each word.
    - The more a word appears locally (meaning frequency within a single symptom description) and the less it appears globally (less frequent across all other symptom descriptions) then the higher its weight. For example, one person describing chicken pox symptoms might write 'red spots on skin' and those words appear frequently in their specific text. If 'spots' doesn't appear as much in other people's symptom descriptions across the dataset, this would give that word a higher weight for this particular symptom text. The ML model will later learn that symptom descriptions with high 'spots' weights tend to correlate with the chicken pox label.
  </Annotation>

  <Annotation id="vocabulary-attribute" lines="98" title="Vectorizer Vocabulary">
    Vectorizer.vocabulary_ is a dictionary that looks something like this:
    `{
    'rash': 342,
    'itchy': 189,
    'red': 401,
    'spots': 512,
    'fever': 98,
    ...
    }`
  </Annotation>

  <Annotation id="creating-transform-log" lines="105-110" title="Creating Transform Log">
    Here we are saving the Vectorizer data in a dictionary to return this data from the function. We will use this during the load phase to save this data to our database. 
  </Annotation>

</AnnotatedCode>

### 1.4 - Load

<AnnotatedCode
  code={load.code}
  language="python"
  startLine={1}
>
</AnnotatedCode>